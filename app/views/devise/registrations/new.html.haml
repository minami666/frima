.body
  .header
    .header__logo
      -# = link_to image_tag(src="sns_top.svg" ), root_path
      = link_to image_tag(src="/" ), root_path

    %nav.progress
      %ul.progress-bar
        %li.step1#active
          会員情報
          .progress-status
          .progress-status_bar
        %li.step2
          電話番号認証
          .progress-status
          .progress-status_bar
        %li.step3
          お届け先住所入力
          .progress-status
          .progress-status_bar
        %li.step4
          支払い方法
          .progress-status
          .progress-status_bar
        %li.step5
          完了
          .progress-status
          .progress-status_bar

          -# #done
          -# #active

  = form_for(resource, as: resource_name, url: '/', method: :get ) do |f|
    .signup
      .signup-title
        会員情報入力：テスト
      .signup__box
        .signup__box-input
          .field
            .field-info__nickname
              %label{for: "nickname" } ニックネーム
              %span.form-require 必須
              %br
            .field-input
            = f.text_field :nickname, class: "input-default", autocomplete: "nickname", placeholder: "例）メルカリ太郎"
          .field
            .field-info__email
              %label{for: "email"} メールアドレス
              %span.form-require 必須
              %br
            .field-input
            = f.email_field :email, class: "input-default", autocomplete: "email", placeholder: "PC・携帯どちらでも可"
          .field
            .field-info__password
              %label{for: "password"} パスワード
              %span.form-require 必須
              %br
            .field-input
            = f.password_field :password, class: "input-default", autocomplete: "password", placeholder: "7文字以上"
            -# .field-input
            -# = f.password_field :password_confirmation, class: "input-default", autocomplete: "off", placeholder: "7文字以上"
            %h3 本人確認
          .login__info
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません

          .field
            .field-info__fullname
              %label{for: "family_name_kanji"} お名前（全角）
              %span.form-require 必須
              %br
            .field-input
            = f.text_field :family_name_knj, class: "input-default-half", autocomplete: "last_name", placeholder: "例）山田"
            = f.text_field :first_name_knj, class: "input-default-half", autocomplete: "first_name", placeholder: "例）彩"
          .field
            .field-info__kana
              %label{for: "family-name-katakana"} お名前カナ（全角）
              %span.form-require 必須
              %br
            .field-input
            = f.text_field :family_name_ktkn, class: "input-default-half", autocomplete: "last_name_kana", placeholder: "例）ヤマダ"
            = f.text_field :first_name_ktkn, class: "input-default-half", autocomplete: "first_name_kana", placeholder: "例）アヤ"
          .field
            .field-info__birth
              %label{for: "birthday"} 生年月日
              %span.form-require 必須
            .field-input
            = f.collection_select :birth, Year.all, :id, :name, class: "birthdate_year"
            年
            = f.collection_select :birth, Month.all, :id, :name, class: "birthdate_month"
            月
            = f.collection_select :birth, Day.all, :id, :name, class: "birthdate_day"
            日


            -# = f.collection_select :birthdate_month, Month.all, :id, :name, class: "birthdate_month"
            -# 月
            -# = f.collection_select :birthdate_day, Day.all, :id, :name, class: "birthdate_day"
            -# 日

            -# = f.text_field :birth, class: "input-default-quarter", autocomplete: "birth", placeholder: "例）1980年"
            -# = f.text_field :birth, class: "input-default-quarter", autocomplete: "birth", placeholder: "例）12月"
            -# = f.text_field :birth, class: "input-default-quarter", autocomplete: "birth", placeholder: "例）23日"

          .form__info
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .attention
            「次へ進む」のボタンを押すことにより、
            = link_to '利用規約', '/credits/new'
            に同意したものとみなします

          .next__btn
            = f.submit '次へ進む', class:"next-btn"
          .about__user-information
            = link_to '本人情報の登録について 〉', '#'
  .footer-user
    %ul
      %li= link_to "プライバシーポリシー", '#'
      %li= link_to "メルカリ利用規約", '#'
      %li= link_to "特定商取引に関する表記", '#'
    .footer-user__link
      = link_to image_tag(src= "//www-mercari-jp.akamaized.net/assets/img/common/common/logo-gray.svg?3296506464", class: "mercari-graylogo", height: 80, width: 60), "/"
      %p © Mercari, Inc.


      -# .field
      -#   .field-info__password-comfirm
      -#     %label{for: "password"} パスワード（確認）
      -#     %span.form-require 必須
      -#     %br

rails g model Address prefecture_id:integer city:string